[package]
name = "syncbook"
version = "0.1.2"
edition = "2021"
description = "Pull and push reMarkable tablet notebooks over SSH"
license = "MIT"

[dependencies]
clap = { version = "4", features = ["derive"] }
anyhow = "1"
dirs = "5"
sha2 = "0.10"
serde_json = "1"

[profile.release]
strip = true
